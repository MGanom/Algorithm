```jsx
function reverse(num) {
  let strnum = String(num);
  strnum = strnum.split('');
  if (strnum[0] === '-') {
    strnum.shift();
    strnum = strnum.reverse();
    strnum.unshift('-');
    strnum = strnum.join('');
    strnum = parseInt(strnum);
  } else {
    strnum = strnum.reverse();
    strnum = strnum.join('');
    strnum = parseInt(strnum);
  }
  console.log(strnum);
}

reverse(-1230);
```
